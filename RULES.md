# Правила алгоритма v1.0

Глобальный стоп: `CP >= 5`.

П1 проходит, когда:
- `EG = 5` или `CS ∈ {8, 9, 10}`;
- `Q >= 7`;
- `BG >= 1.3`.

П2 проходит, когда:
- `EH = 5` или `CT ∈ {8, 9, 10}`;
- `X >= 7`;
- `BF >= 1.3`.

Если проходят обе стороны, выбирается сторона с большей готовой вероятностью: `CV` для П1 или `CW` для П2.

Уровни:
- TOP: от 90%;
- STRONG: 85–89.99%;
- STANDARD: ниже 85%.

Фора первых трёх сетов за последние 5 H2H берётся из `DS`, `DV`, `DY`. Знак сохраняется и считается относительно первого игрока.
